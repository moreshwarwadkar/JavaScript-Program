// DIFFERENCE BETWEEN root.textContent and root.innerHTML

let root = document.getElementById("root");

console.log(root.textContent);
console.log(root.innerText);

/*

If Element is not visible on UI then we can not
*/